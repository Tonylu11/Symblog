{# src/Blogger/BlogBundle/Resources/views/Comment/form.html.twig 

<form action="{{ path('blogger_comment_create', { 'blog_id' : comment.blog.id } ) }}" method="post" class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>#}

{{form_start (form, {'action' : path("blogger_comment_create", {'blog_id' : comment.blog.id}), 
   'method': 'POST', 'attr': {'class': 'blogger'}}) }}
{{form_widget(form)}}
{{form_end(form)}}